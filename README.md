# Moonshot

A small browser-based arcade moon mission game built with HTML5 Canvas, CSS, and vanilla JavaScript.

## Play locally

Open `index.html` in a browser, or run a tiny static server:

```bash
python3 -m http.server 8129
```

Then visit <http://127.0.0.1:8129>.
